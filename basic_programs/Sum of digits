#include<stdio.h>
int main(){
int t, n, a, sum=0, i; //initialise sum=0,otherwise it will contain a garbage value
scanf("%d", &t);
for(i=0; i<t; i++)
{ sum=0;
scanf("%d\n", &n);
while(n!=0)
{
a=n%10;
sum=sum+a;
n=n/10;
}
printf("%d\n", sum);
}
return 0;
}

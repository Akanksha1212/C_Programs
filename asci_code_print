#include <stdio.h>
    
    int main()
    {
        char a;
        printf("Enter the character: ")
        scanf("%c", &a);  
        printf("ASCII value of %c = %d", a, a);
        return 0;
    }

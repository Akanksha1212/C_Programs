#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int n,m;
    printf("enter no of elements in subset  ");
    scanf("%d",&m);
    n = m+1;
    int arr[n];
    ar[0] = 0;

    for(int i=1; i<n; i++)
    {
        printf("enter %d eklement of subset  ");
        scanf("%d",&arr[i]);
    }

    int l = pow(2,m),t;
    int arr2[l];
    arr2[0] = 0;

    for(int i =1; i<n; i++)
    {
        for(int j=1; j<l; j++)
        {
             for(int t=j; )
        }
    }
}
